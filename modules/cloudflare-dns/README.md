# Cloudflare DNS

Installs the reviewed Cloudflare DNS adapter and exposes its configuration in Rootminster Module Settings. The Cloudflare API token remains encrypted in Rootminster's database and is never shipped in this manifest.
