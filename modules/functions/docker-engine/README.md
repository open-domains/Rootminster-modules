# Docker Engine

Mounting the Docker socket grants powerful host access. Use `allowedLabels` to restrict this module to containers explicitly managed by Rootminster, for example `rootminster.managed=true`.
