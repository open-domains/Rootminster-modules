# AI Review feature module

Enable with `FEATURE_MODULES=ai-review`, then set `AI_API_KEY`, `AI_MODEL`, and optionally `AI_BASE_URL` (defaults to `https://api.openai.com/v1`).

Suggestions are advisory. The module has no route that approves, rejects, or mutates DNS.
