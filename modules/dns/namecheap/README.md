# Namecheap DNS

Requires Namecheap API access and an allow-listed public client IP. Namecheap replaces the complete host set on update, so Rootminster must read, merge and submit records atomically.
