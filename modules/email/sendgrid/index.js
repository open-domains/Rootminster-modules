async function send(ctx,message){const res=await ctx.fetch("https://api.sendgrid.com/v3/mail/send",{method:"POST",headers:{Authorization:`Bearer ${ctx.config.apiKey}`,"Content-Type":"application/json"},body:JSON.stringify({personalizations:[{to:(Array.isArray(message.to)?message.to:[message.to]).map(email=>typeof email==="string"?{email}:email)}],from:{email:ctx.config.from,name:ctx.config.fromName},subject:message.subject,content:[{type:message.html?"text/html":"text/plain",value:message.html||message.text}]})});if(!res.ok)throw new Error(`SendGrid HTTP ${res.status}: ${await res.text()}`);return {accepted:true};}
export default {send,async health(ctx){return {ok:Boolean(ctx.config.apiKey&&ctx.config.from)};}};
