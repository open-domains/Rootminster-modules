{
  "id": "smtp",
  "name": "SMTP Email",
  "version": "1.0.0",
  "type": "email-provider",
  "main": "index.js",
  "description": "Standards-based SMTP delivery through Nodemailer.",
  "config": ["SMTP_HOST", "SMTP_PORT", "SMTP_SECURE", "SMTP_USER", "SMTP_PASS", "MAIL_FROM"],
  "requiredConfig": ["SMTP_HOST", "MAIL_FROM"]
}
